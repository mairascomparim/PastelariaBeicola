@model IEnumerable<Beicola.Models.Categorie>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nm_Cat)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.St_Cat)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nm_Cat)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.St_Cat)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Cat">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Cat">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Cat">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
